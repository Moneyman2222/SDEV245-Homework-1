The website when open will ask for a username and password when you type in as an admin (the username is admin, and the password is adminpassword) you can access the admin content and user content. if you type in the user account (the username is user, and the password is userpassword) you have only access to user content. The part of the CIA triad that this code is using is Availability as the program only requires the username and password to access the content.